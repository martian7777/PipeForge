# PipeForge backend â€” core dependencies (Milestone 1-2).
# Install:  pip install -r requirements.txt
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
pydantic>=2.6
pydantic-settings>=2.2
SQLAlchemy>=2.0
pandas>=2.1
numpy>=1.26
openpyxl>=3.1          # Excel reading
pyarrow>=15.0          # Parquet reading
email-validator>=2.1   # pydantic EmailStr

# --- Security / auth ---
PyJWT>=2.8
bcrypt>=4.1
slowapi>=0.1.9         # rate limiting (in-memory or Redis backend)

# Optional: Postgres driver for horizontal scaling (multi-replica).
# psycopg[binary]>=3.1
# Optional: Redis for shared rate-limit storage across replicas.
# redis>=5.0

# --- AutoML training engine (Milestone 3): tabular model zoo ---
scikit-learn>=1.4
xgboost>=2.0
lightgbm>=4.3
joblib>=1.3

# --- Deep learning + time series (Milestone 4) live in requirements-ml.txt ---
