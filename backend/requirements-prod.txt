# Production-only deps (used by the Docker image / horizontal-scaling setup).
psycopg[binary]>=3.1   # PostgreSQL driver (shared DB across replicas)
redis>=5.0             # shared rate-limit storage across replicas
